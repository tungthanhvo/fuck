define(['./module'], function(directives) {
    'use strict';
    directives.directive('employeeProfile', ['$parse', function($parse) {
        return {
            restrict: 'E',
            templateUrl: 'templates/employeeprofile.html',
            controller: "employeeProfileController",
            controllerAs: 'empProfileCtrl',
            bindToController: true
        }
    }]);
});