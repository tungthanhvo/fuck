define(['./module'], function(directives) {
    'use strict';
    directives.directive('recentAssignment', ['$parse', function($parse) {
        return {
            restrict: 'E',
            templateUrl: 'templates/recent_assignment.html',
            controller: "recent_assignmentController",
            controllerAs: 'recentAssignmentCtrl',
            bindToController: true
        }
    }]);
});